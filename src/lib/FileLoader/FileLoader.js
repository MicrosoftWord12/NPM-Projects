const fs = require("fs");
const path = require("path");

const envFile = path.resolve(__dirname, "../Templates/Configs/.env");
const jsonFile = path.resolve(__dirname, "../Templates/Configs/Config.json");

const botFile = path.resolve(__filename);

const createEnvFile = () => {
    fs;
};

const createJsonFile = () => {};
